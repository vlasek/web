

let userName = promt ("Введите имя", "Имя");
function showMessage ()
{

    let userName = promt ("Введите имя", "Имя");
console.log("Привет, " + userName);
}


function release(userName) {
    return `Привет, ${userName}!`;
}

showMessage();